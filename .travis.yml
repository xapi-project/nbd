language: c
sudo: required
install: wget https://raw.githubusercontent.com/ocaml/ocaml-travisci-skeleton/master/.travis-opam.sh
script: bash -ex .travis-opam.sh && bash -ex .coverage.sh
env:
  matrix:
  - OCAML_VERSION=latest
branches:
  only: master
